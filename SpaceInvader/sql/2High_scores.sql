SELECT
	PlayerID
	PlayerName
	MAX(score) AS highScore

CREATE TABLE HighScores (
        PlayerID int,
        PlayerName varchar(225)
        lastScore int,
        Score int,
        DateofScore date,
);







